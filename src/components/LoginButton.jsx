import React from 'react';

const LoginButton = () => {
  return (
    <div>
      <button className="login-button" type="button">
        Login
      </button>
    </div>
  );
};

export default LoginButton;
